extends base

block vars
  - var siteTitle = "Landing"

block content
  include header
  main
    section(class="landing")
      div(class="landing__container container")
        div(class="landing__roomcard")
          input(type="text" id="datepicker" placeholder="Выберите дату")
          script(src="../index.js") <!-- Подключите ваш скомпилированный файл -->
          p(class="landing__text")
  include footer