extends base

include mixin

block vars
  - var siteTitle = "Landing"

block content
  include header
  main
    section(class="landing")
      div(class="landing__container")
        +datepicker
        p(class="landing__text") sdfsdf
  include footer